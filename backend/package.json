{
  "name": "lumio-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lumio backend Worker — Cloud Foundation sprint (no business logic yet).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production",
    "check": "node --check src/index.js",
    "db:migrate:local": "wrangler d1 migrations apply lumio-dev --local",
    "db:migrate:dev": "wrangler d1 migrations apply lumio-dev --remote",
    "db:migrate:staging": "wrangler d1 migrations apply lumio-staging --env staging",
    "db:migrate:production": "wrangler d1 migrations apply lumio-production --env production",
    "db:tables:local": "wrangler d1 execute lumio-dev --local --command=\"SELECT name FROM sqlite_master WHERE type='table' ORDER BY name;\""
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
